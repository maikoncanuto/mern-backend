name: ci-workflow-scan-packages

on:
    workflow_call:

jobs:
    scan-git:
    timeout-minutes: 5
    runs-on: ubuntu-latest
    steps:
        - name: Checkout Code
          uses: actions/checkout@v3

        - name: Workflow to Scan Packages
          run: npm audit
          env:
              CI: true